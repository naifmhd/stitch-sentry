APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=stitch_sentry
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=reverb
# Set to 'local' for local development when S3 is not configured
FILESYSTEM_DISK=s3
QUEUE_CONNECTION=redis

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

WORKOS_CLIENT_ID=
WORKOS_API_KEY=

# S3-compatible storage (AWS S3, Cloudflare R2, DigitalOcean Spaces, etc.)
# Use FILESYSTEM_DISK=local above to skip S3 during local dev
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_URL=
# Uncomment for S3-compatible endpoints (R2, Spaces):
# AWS_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
# AWS_USE_PATH_STYLE_ENDPOINT=true
WORKOS_REDIRECT_URL="${APP_URL}/authenticate"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

REVERB_APP_ID=stitch-sentry
REVERB_APP_KEY=stitch-sentry-key
REVERB_APP_SECRET=stitch-sentry-secret
REVERB_HOST=localhost
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"


# Paddle (Cashier Paddle - Billing)
PADDLE_SELLER_ID=
PADDLE_CLIENT_SIDE_TOKEN=
PADDLE_API_KEY=
PADDLE_RETAIN_KEY=
PADDLE_WEBHOOK_SECRET=
PADDLE_SANDBOX=true

PADDLE_PRICE_STARTER=
PADDLE_PRICE_SHOP=
PADDLE_PRICE_DIGITIZER=

PADDLE_SUBSCRIPTION_NAME=default
PADDLE_PLANS_STRICT=true

# Parser / Renderer Service
PARSER_SERVICE_URL=
PARSER_SERVICE_SECRET=
PARSER_TIMEOUT_SECONDS=20
PARSER_CONNECT_TIMEOUT_SECONDS=5
PARSER_RETRY_TIMES=2
PARSER_RETRY_SLEEP_MS=200
PARSER_MOCK=false
